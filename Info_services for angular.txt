-->we have to install npm command in ubuntu machine (ubuntu ec2 machine)
$sudo apt-get install npm            //install npm command in ubuntu
$sudo npm install bootstrap@4        //install bootstrap 4version
$sudo npm install --save @ng-bootstrap/ng-bootstrap@4       //install ng-bootstrap 4 version

$sudo npm i ng-chat         //install angular google chat


$ng serve                  //run the application







1.bootstap 4

$npm install bootstrap@4

2. ng bootstap 4
$npm install --save @ng-bootstrap/ng-bootstrap@4


3. angular google chat
$npm i ng-chat

-->when ever run the application  in browser we have to run this command in git bash before the open in browser in  Angular

$ng serve                  //run the application


url
localhost:1266

path of project:
E:\sampleproject\my-app